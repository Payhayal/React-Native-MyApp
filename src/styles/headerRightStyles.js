import {StyleSheet} from 'react-native';

const HeaderRightStyles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'row',
    gap: 15,
  },
});
export {HeaderRightStyles};
